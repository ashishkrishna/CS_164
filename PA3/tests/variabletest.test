class Fiatlux {
     a:Int;
     letitbe():Int  {
     {
     let a:Int in 2 * 1 + 2 * 2;
     let a:Int <- let b:Int in 2 + 2 * 3 in 2 + 1;
     let a:Int in if 1 <=2 then a else a fi ;
     let a:Int <- 1*1 in 2 * 1 *5;
     let a:Int in let b:Int in 1;
     let a:Int in let b:Int in 1;1;1;
     }
     };
};

class A {
   set_var(num : Int) : SELF_TYPE {
      let x : Int in x <- 2*3-2;
      ~x;
   };
};