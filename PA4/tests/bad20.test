-- Case branches are tested from most specific to most generic.


class Main inherits IO
{
  main() : Object
  {
    let thing : Object <- (new Int) in
      case thing of
	i : Int => out_string( "obj\n" );
	b : Int => out_string( "int\n" );
      esac
  };
};